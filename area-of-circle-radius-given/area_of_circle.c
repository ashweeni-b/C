// This program calculates area of circle if radius is given

#include <stdio.h>

int main() {
    float radius = 3;

    float area = 3.14 * radius * radius;

    printf("The area of circle is %f", area);

    return 0;
}