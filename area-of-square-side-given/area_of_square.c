// This program calculates area of a square if the side is given

#include <stdio.h>

int main() {
    float side = 5.0;

    float area = side * side;	// Calculates the area of square

    printf("The area of square is %f", area);	// Outputs the area

    return 0;
}